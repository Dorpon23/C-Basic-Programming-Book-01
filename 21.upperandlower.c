#include<stdio.h>
int main()
{
    char ch='p';
    if(ch=='A')
    {
        printf("Leter is upper case %c",ch);
    }
    else if(ch=='b')
    {
        printf("Leter is lower case %c",ch);
    }
   else if(ch=='C')
    {
        printf("Leter is upper case %c",ch);
    }
    else if(ch=='g')
    {
        printf("Leter is lower  case %c",ch);
    }
    else if(ch=='p')
    {
        printf("Leter is lower  case %c",ch);
    }
    else
    {
          printf("Leter is upper case %c",ch);
    }
}
